"""api.services — service layer modules.

M3-1 canonical-layer (PO 2026-06-24) is the first occupant; further
service modules can land here as the system grows. This package
intentionally has no public re-exports — every consumer imports the
explicit submodule (`from api.services.canonical_mapping import ...`).
"""

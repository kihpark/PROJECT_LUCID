"""KnowledgeSpace CRUD routes - /api/spaces.

Scaffold stub (TASK-001). Endpoints land in a later task. See AGENTS.md section 6.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/api/spaces", tags=["spaces"])

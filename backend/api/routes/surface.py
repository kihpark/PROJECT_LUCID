"""Proactive recall route - /api/spaces/{sid}/surface.

Scaffold stub (TASK-001). C3 contextual surfacing lands in a later task.
"""
from fastapi import APIRouter

router = APIRouter(prefix="/api/spaces/{sid}", tags=["surface"])

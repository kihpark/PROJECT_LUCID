"""DEVX-01 — operational liveness modules (smoke, replay drivers).

Anything under `api.ops` is structural: it confirms the stack is
WIRED, not that any particular dataset is present. Data-driven
acceptance lives in `pytest tests/...`.
"""

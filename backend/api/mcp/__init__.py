"""spike/mcp-streamable-http — MCP server stub. NOT mounted in api.main.

This package is a sealed reconnaissance spike. The router is
intentionally not registered with the FastAPI app so the production
request path is unaffected. To run the spike server standalone:
    python -m api.mcp.server
"""

# Lucid backend dependencies.
# Beta storage = Neo4j + FAISS only (DR-005, DR-006). No Qdrant, no Postgres.
# No instaloader (DR-013 - Meta ToS). Embeddings run on a local model (DR-008).

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Data models ---
pydantic>=2.6

# --- Graph + vector storage ---
neo4j>=5.18
faiss-cpu>=1.8
sentence-transformers>=2.6

# --- LLM ---
anthropic>=0.25

# --- Capture / extraction ---
youtube-transcript-api>=0.6
yt-dlp>=2024.4.9
faster-whisper>=1.0
newspaper3k>=0.2.8
readability-lxml>=0.8
beautifulsoup4>=4.12
pdfplumber>=0.11
pillow>=10.0

# --- Scheduling (staleness checker) ---
apscheduler>=3.10

# --- Utilities ---
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# --- Dev tooling (lint + type check) ---
ruff>=0.4
mypy>=1.10

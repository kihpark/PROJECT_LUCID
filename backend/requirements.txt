# Lucid backend dependencies.
#
# Beta storage = Postgres + Elasticsearch (v2, 2026-05-21).
# Neo4j and FAISS retired in Sprint 1A PR-1A-1. apscheduler retired with
# the staleness system (see CONFLICTS.md C-14).
# No instaloader (DR-013 - Meta ToS).

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Data models ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Relational storage + migrations ---
sqlalchemy[asyncio]>=2.0
psycopg2-binary>=2.9
alembic>=1.13

# --- Search + graph adjacency (kNN + nori) ---
elasticsearch>=8.11,<9.0

# --- Auth (Sprint 1B) ---
PyJWT>=2.8
bcrypt>=4.1
email-validator>=2.0  # Pydantic EmailStr validation

# --- LLM ---
anthropic>=0.25

# spike/mcp-streamable-http — Anthropic-maintained MCP Python SDK
mcp>=1.0
openai>=1.30  # text-embedding-3-small for ES kNN (PR-1A-3)

# --- Capture / extraction ---
langdetect>=1.0.9
lxml>=5.0
youtube-transcript-api>=0.6
yt-dlp>=2024.4.9
faster-whisper>=1.0
newspaper3k>=0.2.8
readability-lxml>=0.8
trafilatura>=2.0
beautifulsoup4>=4.12
pdfplumber>=0.11
pillow>=10.0

# --- Utilities ---
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# --- Dev tooling (lint + type check) ---
ruff>=0.4
mypy>=1.10
